import { ReactComponent as Icon} from '../../assets/WrongIcon.svg';

import classes from './WrongIcon.module.scss';

const WrongIcon = () => {

  return (
    <Icon className={classes.WrongIcon}></Icon>
  )
}

export default WrongIcon;